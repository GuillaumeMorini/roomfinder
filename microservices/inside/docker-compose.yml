version: '3'

services:
  inside:
    image: inside
    env_file: .env
    deploy:
      replicas: 2
      update_config:
        parallelism: 2
        delay: 10s
      restart_policy:
        condition: on-failure
      labels:
        - "traefik.backend=inside"
        - "traefik.frontend.rule=Host:inside.cisco.com"
        - "traefik.port=5000"
    networks: 
      - roomfinder-network

networks:  
  roomfinder-network:
    external: true